# SentinelX AI — Phase 2 Backend Dependencies
# Install with: pip install -r backend/requirements.txt

# ── FastAPI core ───────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# ── Database ───────────────────────────────────────────────────
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9       # PostgreSQL driver (binary wheel, no libpq-dev needed)

# ── Phase 1 model dependencies ─────────────────────────────────
# (also listed in ml/requirements.txt — kept here for a self-contained install)
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
joblib>=1.3.0

# ── LLM / AI ───────────────────────────────────────────────────
anthropic>=0.28.0             # Anthropic API client (claude-sonnet-4-6)

# ── RAG / Vector DB ────────────────────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-chroma>=0.1.2
langchain-huggingface>=0.0.3
chromadb>=0.5.0
sentence-transformers>=3.0.0  # all-MiniLM-L6-v2 embeddings (local, no API key)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0                  # required by sentence-transformers

# ── Testing ────────────────────────────────────────────────────
pytest>=8.2.0
pytest-asyncio>=0.23.0
httpx>=0.27.0                 # async HTTP client for FastAPI TestClient

# ── Report Generation ──────────────────────────────────────────
reportlab>=4.0.0
